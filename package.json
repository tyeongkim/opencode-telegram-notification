{
	"name": "opencode-telegram-notification",
	"version": "0.1.3",
	"description": "OpenCode plugin that sends Telegram notifications on session idle, permission requests, and errors",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"files": [
		"dist"
	],
	"keywords": [
		"opencode",
		"plugin",
		"telegram",
		"notification"
	],
	"license": "MIT",
	"scripts": {
		"build": "bun build src/index.ts --outdir dist --target node && tsc --emitDeclarationOnly --noEmit false --outDir dist",
		"typecheck": "tsc --noEmit",
		"prepublishOnly": "bun run typecheck && bun run build"
	},
	"dependencies": {
		"@opencode-ai/plugin": "latest"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
